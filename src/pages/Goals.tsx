import { PagePlaceholder } from '../components/PagePlaceholder';

export function Goals() {
  return (
    <PagePlaceholder
      title="Sparziele"
      hint="Ziele anlegen, Einzahlungen buchen und den Fortschritt verfolgen (Phase 6)."
    />
  );
}
